const { useState, useEffect } = require("react")

const usePost = () => {
    const [isPending, setPending] = useState(true)
    const onSubmitPost = (e) => {

        //await upload image => [url]
        //await upload form => Response.ok => isPending = false

    }
}